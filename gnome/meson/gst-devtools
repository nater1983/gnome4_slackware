mkdir build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${GNOME_BUILD_ARGS[@]}" \
    $GNOME_OPT_ARGS \
     -Ddoc=disabled \
    -Ddebug_viewer=enabled \
    -Ddots_viewer=disabled
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
