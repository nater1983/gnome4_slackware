mkdir build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${GNOME_BUILD_ARGS[@]}" \
    $GNOME_OPT_ARGS \
    -Dtests=false
  meson compile src/gtex/Gtex-1.gir
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
