mkdir build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${GNOME_BUILD_ARGS[@]}" \
    $GNOME_OPT_ARGS \
    -Dbash_completion=false \
    -Dgtk_doc=true \
    -Dman=true
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
