mkdir build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${GNOME_BUILD_ARGS[@]}" \
    $GNOME_OPT_ARGS \
    -Dudev=/lib/udev/rules.d \
    -Dv4l2=true \
    -Dtracing=disabled \
    -Dtest=true
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
