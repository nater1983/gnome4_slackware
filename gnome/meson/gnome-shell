mkdir build
cd build
  CFLAGS="$SLKCFLAGS -Wl,-Bsymbolic-functions -fno-semantic-interposition" \
  CXXFLAGS="$SLKCFLAGS -Wl,-Bsymbolic-functions -fno-semantic-interposition" \
  meson setup ..\
    "${GNOME_BUILD_ARGS[@]}" \
    $GNOME_OPT_ARGS \
    -Dgtk_doc=false \
    -Dman=true \
    -Dtests=false \
    -Dsystemd=false
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
