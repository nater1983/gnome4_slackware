mkdir cmake-build
cd cmake-build
  cmake .. \
    "${GNOME_CMAKE_ARGS[@]}" \
     -G Ninja \
     -W no-dev

mkdir -p "$PKG/usr/include"
cp -r ../glm "$PKG/usr/include/"

install -vDm 0644 cmake/glm/glmConfig-version.cmake -t $PKG/usr/share/cmake/glm/
install -vDm 0644 cmake/glm/glmConfig.cmake -t $PKG/usr/share/cmake/glm/
