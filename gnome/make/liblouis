CFLAGS="$SLKCFLAGS" \
CXXFLAGS="$SLKCFLAGS" \
./configure \
    "${GNOME_MAKE_ARGS[@]}" \
    --docdir=/usr/doc/$PRGNAM-$MODULAR_PACKAGE_VERSION \
    --enable-ucs4
make
make install DESTDIR=$PKG

cd python
python3 setup.py install --root=$PKG
cd ..
