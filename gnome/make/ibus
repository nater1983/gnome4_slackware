CFLAGS="$SLKCFLAGS" \
CXXFLAGS="$SLKCFLAGS" \
../configure \
    "${GNOME_MAKE_ARGS[@]}" \
      --enable-python-library \
      --with-python=python3 \
      --with-unicode-emoji-dir=$GNOME_BUILD_DIR/${PKGNAME}-${MODULAR_PACKAGE_VERSION} \
      --with-ucd-dir=$GNOME_BUILD_DIR/${PKGNAME}-${MODULAR_PACKAGE_VERSION} \
      --disable-systemd-services
../make
../make install DESTDIR=$PKG
