export release=1
export verbose=1
export interpreter=1
export CXXFLAGS="$SLKCFLAGS"
export LLVM_CONFIG=$(find /usr/bin -name "llvm-config*" -print -quit)
make
make docs

install -D -m 755 .build/gi-crystal $PKG/usr/bin/gi-crystal
